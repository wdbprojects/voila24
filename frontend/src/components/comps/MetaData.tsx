import { Helmet } from "react-helmet";

const MetaData = ({ title }) => {
  return (
    <div>
      {/* <Helmet>
        <title>{`${title} - ShopIt`}</title>
      </Helmet> */}
    </div>
  );
};

export default MetaData;
